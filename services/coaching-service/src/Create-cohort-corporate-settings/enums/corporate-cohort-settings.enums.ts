export enum YesNo {
  NO = 0,
  YES = 1,
}

export enum CohortType {
  COACHING = 1,
  MENTORING = 2,
}

export enum CoachSearchType {
  EXTERNAL = 0,
  INTERNAL = 1,
}

export enum Status {
  INACTIVE = 0,
  ACTIVE = 1,
}

export enum EnableDuration {
  DISABLED = 0,
  ENABLED = 1,
} 